// Stripe client — Phase 1 deliverable. Phase 0 leaves a placeholder so the integrations
// directory layout matches the build plan. Invoice issuing and payment ingestion arrive with
// the Controller in Phase 1 and route through the outbox for exactly-once guarantees.

export const STRIPE_NOTE = 'Stripe integration arrives in Phase 1 via the outbox.';
