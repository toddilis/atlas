// Conversational assistant — Phase 2 deliverable.
//
// Contract (locked): NL over memory + canonical read-models, Claude-driven, READ-ONLY. The
// assistant must never touch money math, ledger postings, or other deterministic boundaries
// (§10). Phase 0 keeps this file as a placeholder so the platform layout matches the build
// plan; the actual implementation arrives with the console in Phase 2.

export const ASSISTANT_NOTE = 'Assistant is read-only and arrives in Phase 2.';
