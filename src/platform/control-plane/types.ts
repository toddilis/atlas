export type RiskTier      = 'auto' | 'notify' | 'approve_required';
export type ApprovalState = 'pending' | 'approved' | 'rejected' | 'expired';
export type AuditOutcome  = 'success' | 'failure' | 'blocked';
