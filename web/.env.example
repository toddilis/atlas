# Atlas operator console — local dev only. Service-role key bypasses RLS so the
# console can read every row in the org without auth (v1: single operator).
# All env vars are server-side; never expose to client components.

SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=
ATLAS_ORG_ID=
