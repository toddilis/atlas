// Route-level loading state (PR-N) — shown while force-dynamic server components fetch.

export default function Loading() {
  return (
    <div className="py-16 text-center text-sm text-zinc-500 dark:text-zinc-500">
      Loading…
    </div>
  );
}
